class User{
  String email;
  String firstName;
  String lastName;
  String phone;
  String address;
  String restId;

  User(this.email, this.firstName, this.lastName, this.phone, this.address, this.restId);
}